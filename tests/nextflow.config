// Global nf-test config. Per-test Nextflow configuration is supplied by the
// `config` directive inside each *.nf.test file (e.g. the example pipeline
// loads examples/rnaseq-pipeline/nextflow.config, which declares the plugin).
